# UAT IP allowlist (UAT nginx in /opt/ie-orbit-uat). Unlisted clients get 403.
# Edit this file, then reload UAT nginx — do not touch prod.
# Find your public IPv4 from the machine that needs access: curl -4 ifconfig.me
#
# allow 203.0.113.10;   # Sanket home
# allow 198.51.100.20;  # office
deny all;
