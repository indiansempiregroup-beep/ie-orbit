To run the docker:







docker compose up -d







# If phone/API can't connect, restart backend then retest:



# docker compose restart backend



# curl http://172.20.10.14:8000/api/v1/health/











# After laptop reboot / if Expo says "Cannot find module":



# (run once from repo root — restores node_modules; workspace uses hoisted pnpm)



cd "D:\Indians Empire\IE Platform\ie-platform"



pnpm install











To run OPS in the browser (Expo RN-web — same app as mobile):

cd apps/ops-mobile
corepack pnpm web
# or: corepack pnpm start  then press "w"
# Open the URL Expo prints (often http://localhost:8082)
# Login with the same credentials as before (e.g. pilot-owner@ieplatform.local / PilotPass123!)
#
# Platform-admin-only accounts now open Platform Admin inside Expo (Tenants / Coupons / Audit).
# Vite web/ (:3000) is still used for marketing/register and remaining admin pages
# (subscriptions, packages, tickets, branding, etc.) until those are fully ported.


To run the OPS-Mobile (phone on same Wi-Fi / hotspot — use your PC LAN IP, not localhost)







# Show current Wi-Fi IP (use this value below — NOT a public IP like 106.x.x.x):



# Get-NetIPAddress -AddressFamily IPv4 | Where-Object { $_.InterfaceAlias -eq 'Wi-Fi' } | Select-Object -ExpandProperty IPAddress







# If you ever see "expo not found" / missing @expo/cli, repair install first:
# cd "D:\Indians Empire\IE Platform\ie-platform"
# corepack pnpm install --filter @ie-platform/ops-mobile...

cd "D:\Indians Empire\IE Platform\ie-platform\apps\ops-mobile"
$env:REACT_NATIVE_PACKAGER_HOSTNAME="172.20.10.14"
$env:EXPO_PUBLIC_API_BASE_URL="http://172.20.10.14:8000/api/v1"
$env:EXPO_NO_METRO_WORKSPACE_ROOT="1"
pnpm.cmd start -- --clear --lan







# QR should show exp://172.20.10.14:8082 (NOT 127.0.0.1)







To run the Customer-Mobile (phone on same Wi-Fi / hotspot)







cd "D:\Indians Empire\IE Platform\ie-platform\mobile"
$env:REACT_NATIVE_PACKAGER_HOSTNAME="172.20.10.14"
$env:EXPO_PUBLIC_API_BASE_URL="http://172.20.10.14:8000/api/v1"
$env:EXPO_PUBLIC_FLAVOR_KEY="enjoy-pet-hub-and-kennels-enjoy-pet-hub-and-kennels"
$env:EXPO_PUBLIC_APP_NAME="Enjoy pet hub and kennels"
pnpm.cmd exec expo start --port 8083 --clear --lan







# QR should show exp://172.20.10.14:8083 (NOT 127.0.0.1)







# If Expo says "Unable to load app" / "Network request failed", or login fails:



# 1) On your iPhone Safari open: http://172.20.10.14:8000/api/v1/health/



#    - If Safari also fails, Windows Firewall is blocking the phone (most common).



# 2) Open PowerShell AS ADMINISTRATOR and run ALL rules:



#    New-NetFirewallRule -DisplayName "IE Platform Backend 8000" -Direction Inbound -Action Allow -Protocol TCP -LocalPort 8000 -Profile Any



#    New-NetFirewallRule -DisplayName "IE Platform Expo Ops 8082" -Direction Inbound -Action Allow -Protocol TCP -LocalPort 8082 -Profile Any



#    New-NetFirewallRule -DisplayName "IE Platform Expo Customer 8083" -Direction Inbound -Action Allow -Protocol TCP -LocalPort 8083 -Profile Any



# 3) Restart Expo with the env vars above, then reload the app in Expo Go.







# If LAN still fails, use tunnel instead:



# pnpm.cmd exec expo start --port 8083 --clear --tunnel











GIT Commands



git add .



git commit -m "Your descriptive commit message"



git push origin main

